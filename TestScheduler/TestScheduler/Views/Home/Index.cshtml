@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome user @ViewBag.username</h1>
    <ul>
        @foreach (var x in ViewBag.userType)
        {
            <li>@x</li>
        }
    </ul>
</div>
